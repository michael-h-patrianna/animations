import type { AnimationMetadata } from '@/types/animation'

export const metadata: AnimationMetadata = {
  id: 'button-effects__split-reveal',
  title: 'Split Reveal',
  description:
    'Component + CSS for button text splitting apart to reveal hidden content. Props: topLabel, bottomLabel, revealContent, duration, className. Custom property: --pf-split-reveal-duration.',
  disableReplay: true,
  tier: 2,
}
