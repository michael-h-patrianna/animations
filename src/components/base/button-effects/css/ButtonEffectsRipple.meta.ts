import type { AnimationMetadata } from '@/types/animation'

export const metadata: AnimationMetadata = {
  id: 'button-effects__ripple',
  title: 'Ripple',
  description:
    'Wrapper component + CSS for Material Design-style click ripple. Custom properties: --pf-ripple-color, --pf-ripple-duration.',
  disableReplay: true,
  tier: 2,
}
